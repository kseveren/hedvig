import { Wrapper, Container } from './styles';
import { Perils } from '@components/Perils';

export const PerilsSection = () => (
  <Wrapper>
    <Container>
      <Perils />
    </Container>
  </Wrapper>
);
